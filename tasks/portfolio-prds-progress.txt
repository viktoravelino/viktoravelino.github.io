## Codebase Patterns
- **Layout**: Use `main.astro` layout wrapping all pages; it includes global CSS, meta tags, and `class="dark"` on `<html>`
- **Fonts**: Google Fonts loaded in `main.astro` head — Instrument Serif (display/headings) + Geist (body text). Applied via inline `style` attribute on elements.
- **Astro components for static sections**: Hero section is `.astro` since it's static content. React components only needed for interactive parts (e.g., SocialLinks needs `client:load` for lucide-react icons).
- **Data files**: Keep section data in `src/data/<section>.ts` with typed exports. Import into both Astro and React components.
- **CSS animations**: Hero uses CSS keyframe animations with `--delay` CSS variable for stagger (`hero-fade-in` class). Define keyframes + utility classes in `global.css`.
- **shadcn Button `asChild`**: Use `asChild` prop with `<a>` tags for link buttons — avoids nesting `<button>` inside `<a>`.
- **Tailwind v4**: Uses `@theme inline` block in global.css for custom theme values. No `tailwind.config.js` — everything is CSS-based.
- **Build check**: Always run `npm run build` to verify — no linter/tests configured yet.
- **Scroll reveal pattern**: Shared `useScrollReveal` hook in `src/hooks/use-scroll-reveal.ts` + CSS classes `scroll-reveal` / `scroll-reveal-visible` in global.css. Use `--delay` CSS variable for stagger (same pattern as hero). Apply to any section that needs scroll-triggered animations.
- **React sections with client:visible**: Interactive sections (skills, experience, projects) use React components with `client:visible` directive in Astro — only hydrates when scrolled into view.
- **Timeline pattern**: Per-item scroll reveals (each TimelineItem gets its own `useScrollReveal` ref) vs. section-level (SkillsSection uses one ref for all cards). Use per-item when items are spaced far apart vertically.
- **Badge component**: Installed via `npx shadcn@latest add badge --yes`. Use `variant="secondary"` for tech tags.

---

## 2026-02-09 - PRD 1: Hero Section
- Implemented full hero section with editorial/refined monochrome design direction
- Files created:
  - `src/data/hero.ts` — hero content data (greeting, name, role, description, CTAs, socials)
  - `src/components/social-links.tsx` — React component rendering social icon buttons via shadcn Button ghost variant
  - `src/components/sections/hero-section.astro` — full hero layout with left-aligned editorial design
- Files modified:
  - `src/layouts/main.astro` — added `class="dark"` to html, meta/og tags, Google Fonts (Instrument Serif + Geist)
  - `src/styles/global.css` — smooth scroll, hero-fade-in keyframes (with blur dissolve), hero-line-grow animation, glow pulse, grain texture overlay
  - `src/pages/index.astro` — replaced placeholder with Layout + HeroSection composition
- **Design choices**: Left-aligned editorial layout (not centered), Instrument Serif for name/role, Geist for body, decorative gradient line, ambient glow offset right, SVG grain texture
- **Learnings for future iterations:**
  - Instrument Serif italic works well for role/subtitle — creates nice contrast with the regular weight name
  - `client:load` is needed for SocialLinks because lucide-react icons require JS hydration
  - The `hero-grain::before` pseudo-element needs `pointer-events: none` and `z-index: 1` to not block interactions
  - Stagger delay formula: `calc(var(--delay) * 0.15s + 0.2s)` gives a nice pace for 6-7 elements
  - The layout removed the unused `content` prop from main.astro — future sections should not pass props to the layout
---

## 2026-02-09 - PRD 2: Skills Section
- Implemented categorized skills grid with scroll-reveal animations
- Files created:
  - `src/hooks/use-scroll-reveal.ts` — reusable Intersection Observer hook (threshold 0.1, triggerOnce)
  - `src/data/skills.ts` — skill categories data (Frontend, Backend, Tools & DevOps) with Lucide icons
  - `src/components/skill-category-card.tsx` — card component using shadcn Card with icon header + 2-col skill grid
  - `src/components/sections/skills-section.tsx` — section layout with heading, responsive 3-col grid, scroll-reveal
  - `src/components/ui/card.tsx` — shadcn Card component (installed via `npx shadcn@latest add card`)
- Files modified:
  - `src/styles/global.css` — added `scroll-reveal` keyframes and utility classes (fade-in + slide-up + blur, staggered via `--delay`)
  - `src/pages/index.astro` — added `<SkillsSection client:visible />` below HeroSection
  - `tasks/portfolio-prds.md` — marked PRD 2 as `passes: true`
- **Learnings for future iterations:**
  - The `scroll-reveal` / `scroll-reveal-visible` CSS pattern mirrors the hero's `hero-fade-in` approach but is triggered by JS (Intersection Observer) instead of on-load
  - `useScrollReveal` hook observes a container ref — all children use the same `isVisible` state with staggered `--delay` values
  - shadcn Card component installs cleanly via `npx shadcn@latest add card --yes`
  - Skills data uses Lucide icon components directly (not strings) — `icon: Code2` not `icon: "Code2"`
  - The `font-serif` class (Instrument Serif) works well for section headings, maintaining consistency with the hero
---

## 2026-02-09 - PRD 3: Work Experience Section (Timeline)
- Implemented vertical timeline with alternating left/right cards on desktop, left-aligned on mobile
- Files created:
  - `src/data/experience.ts` — experience entries data (3 placeholder entries with company, role, dates, bullets, technologies)
  - `src/components/timeline-item.tsx` — timeline item with marker, connecting line, card with badges; per-item scroll reveal
  - `src/components/sections/experience-section.tsx` — section wrapper with heading and timeline container
  - `src/components/ui/badge.tsx` — shadcn Badge component (installed via `npx shadcn@latest add badge`)
- Files modified:
  - `src/pages/index.astro` — added `<ExperienceSection client:visible />` below SkillsSection
  - `tasks/portfolio-prds.md` — marked PRD 3 as `passes: true`
- **Design choices**:
  - Desktop uses CSS grid `grid-cols-[1fr_auto_1fr]` for center-aligned timeline with alternating cards
  - Mobile uses flexbox with marker + line on the left, card on the right
  - Current role ("Present") gets a pulsing outer ring via `animate-pulse` on a scaled pseudo-element
  - Bullet points use em dash (`—`) prefix for minimal aesthetic
- **Learnings for future iterations:**
  - Per-item `useScrollReveal` is better than section-level for vertically-spaced items — each card animates independently as it enters viewport
  - The `md:hidden` / `hidden md:block` pattern works well for completely different mobile vs desktop layouts
  - `font-mono` for date ranges gives nice contrast with the serif headings
  - Badge `variant="secondary"` matches the dark theme well for tech tags
---
